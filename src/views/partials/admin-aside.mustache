<!-- MENU SIDEBAR-->
    <aside class="menu-sidebar2">
        <div class="logo">
            <a href="#">
                <img src="/static/admin/images/icon/logo-white.png" alt="Cool Admin" />
            </a>
        </div>
        <div class="menu-sidebar2__content js-scrollbar1">
            <div class="account2">
                <div class="image img-cir img-60">
                    <img src="{{user.image}}" alt="John Doe" />
                </div>
                <h4 class="name">{{user.name}}</h4>
                <a href="#">Sign out</a>
            </div>
            <nav class="navbar-sidebar2">
                <ul class="list-unstyled navbar__list">
                    {{#navItems}}
                    <li>
                        <a href="{{link}}">
                            <i class="fas fa-{{icon}}"></i>{{ title }}</a>
                    </li>
                    {{/navItems}}
                </ul>
            </nav>
        </div>
    </aside>
<!-- END MENU SIDEBAR-->